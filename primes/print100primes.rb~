#!/usr/bin/ruby

def is_prime?(test, primes)
  primes.each do |factor|
    return 0 if test % factor == 0
  end
  return 1
end

primes = [2]
candidate = 3
while primes.length < 100
  primes << candidate if is_prime?(candidate, primes)
  candidate += 2;
end

puts primes
